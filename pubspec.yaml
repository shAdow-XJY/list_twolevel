name: list_twolevel
description: Two type listbuilder easy to use, fill with Map<> Obj.
version: 0.0.3
homepage: https://github.com/shAdow-XJY/list_twolevel

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

